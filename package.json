{
  "name": "policy-change-monitor",
  "version": "0.1.0",
  "description": "Apify Actor that monitors public policy and contract pages for material changes.",
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test",
    "check": "node --check src/main.js"
  },
  "dependencies": {
    "apify": "^3.7.2",
    "cheerio": "^1.2.0"
  }
}
